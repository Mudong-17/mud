import { cyan } from 'kolorist';
import { dependencies } from './dependencies';

export const variant: Variant[] = [
  {
    name: 'Vue+TSX',
    value: 'vue-tsx',
    color: cyan,
    dependencies,
  },
];
